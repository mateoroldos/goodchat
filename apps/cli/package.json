{
  "name": "cli",
  "type": "module",
  "bin": {
    "goodbot": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "check-types": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "@goodbot/core": "workspace:*",
    "@goodbot/plugins": "workspace:*"
  },
  "devDependencies": {
    "@goodbot/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:",
    "vitest": "^4.1.0"
  }
}
