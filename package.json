{
  "name": "goodchat",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "examples/*",
      "packages/*"
    ],
    "catalog": {
      "dotenv": "^17.2.2",
      "zod": "^4.1.13",
      "typescript": "^5",
      "@types/bun": "^1.3.4"
    }
  },
  "type": "module",
  "scripts": {
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "dev": "turbo dev",
    "dev:apps": "turbo dev --filter=./apps/*",
    "dev:packages": "turbo dev --filter=./packages/*",
    "build": "turbo build && node ./scripts/copy-web-assets.mjs",
    "check-types": "turbo check-types",
    "dev:web": "turbo -F web dev",
    "dev:server": "turbo -F server dev",
    "check": "ultracite check",
    "check:knip": "bunx --bun knip",
    "check:all": "bun run check && bun run check-types && bun run check:knip",
    "ci:version": "changeset version && node ./scripts/update-create-cli-versions.mjs && bun update",
    "ci:publish": "for dir in packages/* apps/create-cli apps/goodchat-cli; do (cd \"$dir\" && bun publish || true); done && changeset tag",
    "fix": "ultracite fix",
    "release": "bun run build && bun run ci:publish",
    "test": "bun run test:unit && bun run test:integration",
    "test:unit": "vitest run --config vitest.unit.config.ts",
    "test:unit:watch": "vitest --config vitest.unit.config.ts",
    "test:integration:sqlite": "bun test packages/adapter-sqlite/tests/integration",
    "test:integration": "node scripts/run-integration-tests.mjs",
    "test:migrations": "bunx turbo build --filter=@goodchat/core && node scripts/check-test-migrations.mjs",
    "test:migrations:update": "bunx turbo build --filter=@goodchat/core && bunx drizzle-kit generate --schema packages/adapter-postgres/tests/schema.ts --out packages/adapter-postgres/tests/drizzle --dialect postgresql && bunx drizzle-kit generate --schema packages/adapter-mysql/tests/schema.ts --out packages/adapter-mysql/tests/drizzle --dialect mysql && bunx drizzle-kit generate --schema packages/adapter-sqlite/tests/schema.ts --out packages/adapter-sqlite/tests/drizzle --dialect sqlite",
    "test:db:up": "docker compose -f docker-compose.test.yml up -d",
    "test:db:down": "docker compose -f docker-compose.test.yml down -v",
    "ruler:apply": "bunx @intellectronica/ruler@latest apply --local-only"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@changesets/cli": "^2.30.0",
    "@goodchat/typescript-config": "workspace:*",
    "@types/bun": "^1.3.11",
    "@vitest/ui": "^4.1.3",
    "dotenv": "^17.4.1",
    "drizzle-kit": "^0.31.10",
    "lefthook": "latest",
    "turbo": "^2.9.5",
    "typescript": "^5.9.3",
    "ultracite": "7.2.4",
    "vitest": "^4.1.3"
  },
  "packageManager": "bun@1.3.4"
}
